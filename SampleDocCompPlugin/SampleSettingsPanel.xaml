<UserControl x:Class="SampleDocCompPlugin.SampleSettingsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SampleDocCompPlugin" 
             xmlns:commoncontrols="clr-namespace:Document_Companion.CommonControls;assembly=Document_Companion_Api"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <Label Content="Sample Settings" Grid.Row="0"></Label>
            <commoncontrols:PropertyGrid SelectedObject="{Binding Settings}" Grid.Row="1"></commoncontrols:PropertyGrid>
        </Grid>
    </Grid>
</UserControl>
